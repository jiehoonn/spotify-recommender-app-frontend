# ML Repository API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Database Configuration (fallback if API is unavailable)
DATABASE_URL="file:./data/ratings.db"

# Production API (uncomment and update when deploying)
# NEXT_PUBLIC_API_BASE_URL=https://your-ml-api.railway.app
